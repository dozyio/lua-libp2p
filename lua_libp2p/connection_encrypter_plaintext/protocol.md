# lua_libp2p.connection_encrypter_plaintext.protocol

Plaintext transport handshake helpers.

### Libp2pPlaintextExchange

 Plaintext transport handshake helpers.

```lua
Libp2pPlaintextExchange
```

 Plaintext transport handshake helpers.

#### Fields

- `peer_id`: `string` - Peer ID text.
- `public_key`: `string` - Public key protobuf bytes.

