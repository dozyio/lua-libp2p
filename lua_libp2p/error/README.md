# lua_libp2p.error

Structured error helpers.

### Libp2pError

```lua
Libp2pError
```

#### Fields

- `context`: `table` - Structured context payload.
- `kind`: `string` - Error kind/category.
- `message`: `string` - Human-readable message.

