# lua_libp2p.muxer

Stream muxer registry.

### Libp2pMuxerConfig

 Stream muxer registry.

```lua
Libp2pMuxerConfig
```

 Stream muxer registry.

#### Fields

- `yamux`: `boolean?` - Enable yamux (`/yamux/1.0.0`). Default: true.

